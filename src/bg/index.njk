---
layout: base.njk
title: Bulgarian Home
permalink: /bg/
---

{% include "header.njk" %} {# Include header here too for the index page #}

<p>Bulgarian intro text block 1.</p>

<p>Bulgarian intro text block 2.</p>

<hr> {# Horizontal line #}

<h2>Bulgarian Posts</h2>
{% set posts = collections.bgPosts %} {# Assign the Bulgarian posts collection #}
{% include "post-list.njk" %}

<div class="callout" onclick="this.classList.toggle('expanded')"> {# Expandable callout #}
    <div class="callout-title">Click to Expand</div>
    <div class="callout-content">
        <p>Expandable content text.</p>
    </div>
</div>

<style>
    /* Minimal CSS for callout and tags */
    .callout {
        border: 1px solid #ccc;
        padding: 10px;
        margin: 10px 0;
        cursor: pointer;
        overflow: hidden;
        transition: max-height 0.3s ease-out;
        max-height: 40px; /* Collapsed height */
    }
    .callout.expanded {
        max-height: 500px; /* Expanded height (adjust as needed) */
        transition: max-height 0.3s ease-in;
    }
    .callout-title {
        font-weight: bold;
    }
    .callout-content {
        margin-top: 10px;
    }
</style>
